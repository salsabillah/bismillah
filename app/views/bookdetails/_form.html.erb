<%= form_with(model: bookdetail, local: true) do |form| %>
  <% if bookdetail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bookdetail.errors.count, "error") %> prohibited this bookdetail from being saved:</h2>

      <ul>
      <% bookdetail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.number_field :amount %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
